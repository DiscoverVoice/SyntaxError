# SyntaxError

# 1. Install

## 1.1 Install conda environment for python projects

```bash
conda env create -f environment.yml
conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
```

## 1.2 Install npm modules for JS/TS projects

```bash
npm install
```
